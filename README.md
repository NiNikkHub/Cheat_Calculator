# Исправления практических заданий

Проверены практические задания из билетов №1–16 и повторяющихся билетов №17–30 по фотографиям исходных билетов.

Исправлено:
- билет №2 и его повтор №18: в исходном ряду 23 значения, а не 24;
- частота оценки 4 равна 3, а не 4;
- пересчитаны среднее, выборочная и исправленная дисперсии.

Дополнительно исправлен `app/manifest.ts`: добавлен `dynamic = "force-static"`, чтобы проект с `output: "export"` корректно собирался для GitHub Pages.
